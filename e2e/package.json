{
  "name": "gitchef-e2e",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "WebDriver e2e tests. tauri-driver has no macOS WKWebView driver, so these run in CI on Linux (and could on Windows) - never on a macOS dev machine.",
  "scripts": {
    "test": "wdio run wdio.conf.js"
  },
  "devDependencies": {
    "@wdio/cli": "^9.19.0",
    "@wdio/local-runner": "^9.19.0",
    "@wdio/mocha-framework": "^9.19.0",
    "@wdio/spec-reporter": "^9.19.0"
  }
}
